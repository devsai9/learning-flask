<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results</title>
    <style>
        #container {
            padding: 20px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            position: absolute;
            text-align: center;
        }

        #return {
            text-decoration: none;
            color: black;
            padding: 5px 10px;
            background-color: #f1f1f1;
            border: 1px solid black;
            border-radius: 5px;
        }

        ul {
            text-align: left;
        }
    </style>
</head>
<body>
    <a href="/" id="return">&lt; Go back</a>
    <div id="container">
        <h2>Prediction: {{ result }}</h2>
        <p>Model #{{ model }}</p>
        {% if image_path %}
            <p>Uploaded Image:</p>
            <img src="{{ image_path }}" alt="Uploaded Image" width="200" />
        {% endif %}
        {% if predictions %}
            <p>Prediction Confidences:</p>
            <ul>
                {% for index, prediction in enumerate(predictions) %}
                    {% if index == result %}
                        <li><b>Model #{{ model }} was {{ prediction * 100 }}% sure/confident that the image was of a {{ index }}</b></li>
                    {% else %}
                        <li>Model #{{ model }} was {{ prediction * 100 }}% sure/confident that the image was of a {{ index }}</li>
                    {% endif %}
                {% endfor %}
            </ul>
        {% endif %}
    </div>
</body>
</html>